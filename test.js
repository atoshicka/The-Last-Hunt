// 4. Отрисовка конкретного кадра призрака из спрайт-листа
    const iconWidth = 16;  // Ширина иконки на экране (чуть меньше, чтобы не растягивать)
    const iconHeight = 32; // Высота иконки на экране (сохраняем пропорции 1:2)
    
    // Предполагаем, что один кадр в файле имеет размер 32x64 пикселя
    const sourceX = 0;     // 0 — берем первого (левого) призрака. Если нужен второй — поставь 32
    const sourceY = 0;     // Верхняя граница кадра
    const sourceWidth = 32; // Ширина одного кадра внутри самого файла
    const sourceHeight = 64;// Высота одного кадра внутри самого файла

    ctx.drawImage(
      waveGhostImg, 
      sourceX, sourceY,         // Координаты начала кадра на картинке-файле
      sourceWidth, sourceHeight, // Размеры кадра, который мы вырезаем из файла
      ghostX - iconWidth / 2,   // Центрируем иконку на полоске по X
      ghostY - iconHeight / 2,  // Центрируем иконку на полоске по Y
      iconWidth, iconHeight     // Итоговый размер иконки, который отрендерится в игре
    );